code: GEO1
model: configs/coffee_fg/models/yolo26n-p3.yaml
geometry_conditioning:
  hidden_dim: 32
  max_normalized_side: 2.0
  max_aspect_ratio: 10.0
  target_size_classes_only: true
train:
  epochs: 50
  imgsz: 640
  batch: 16
  workers: 2
  patience: 15
  optimizer: auto
  pretrained: true
  cache: false
  close_mosaic: 10
  max_det: 500
