arm: S1
hypothesis: shared_semantic_calibration_of_msda_offset_attention_value
sscb:
  mode: calibrated
  hidden_dim: 64
  sampling_points: 4
  max_offset_pixels: 2.0
  semantic_aux_weight: 0.2
  correction_scale: 1.0
train:
  epochs: 50
  imgsz: 640
  batch: 16
  seed: 42
