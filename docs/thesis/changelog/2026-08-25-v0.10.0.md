# Thesis Proposal Milestone — v0.10.0

Date: 2026-08-25

## Bab II normalization

- Added `COF-17` García et al. 2019 as a dedicated classical coffee-inspection source.
- Added `proposal/04_02_INSPECTION_QUALITY_NORMALIZED.md` to replace the citation-recycling hotspot in §2.2.
- `COF-07` Kesiman and `COF-08` Arwatchananukul are now reserved for their stronger roles in taxonomy (§2.1) and fine-grained diagnosis (§2.6), rather than generic manual-inspection evidence.
- Closed the `Computers and Electronics in Agriculture` quartile audit as Q1 using 2024 journal metrics; retained Digital Signal Processing as Q2 for the FE-YOLO row.
- Updated the modular §2.9 table accordingly.

## Bab III foundation

Added `foundation/07_USU_BAB3_PATTERN.md` to freeze the campus-style methodology sequence:

```text
architecture
-> dataset
-> split/audit
-> AF2 treatment
-> YOLO26
-> experimental arms
-> training
-> evaluation
-> error/mechanism analysis
-> environment
```

## Bab III proposal draft

Added `proposal/05_METHODOLOGY.md`, grounded in:

- frozen direct-from-pretrained protocol;
- current AF2 config;
- current `operator.py` implementation;
- grouped Faruq-v3 development contract.

Important corrections captured in the methodology:

- direct protocol uses 50 epochs / workers 2 / patience 15;
- older `D0_yolo26n.yaml` values 100 / 4 / 20 are not authoritative for direct-AF2;
- AF2 active parameters are patch=32, overlap=.50, gamma=.10, 360 angular bins;
- `radius_ratio=.05` exists in the shared AFAB config but is **inactive in `mode=af2`**;
- AF2 RGB channels are processed independently;
- angle binning and overlap averaging are explicit transfer/implementation choices;
- AF2 is parameter-free but not compute-free;
- Bottom-3 and Worst-class are study-defined summary metrics, not standard COCO metrics;
- seed 42 is preliminary; seeds 123 and 2026 are planned confirmation runs.

## Protocol audit

Added `sources/BAB3_PROTOCOL_AUDIT.md`.

It records:

- source-of-truth hierarchy;
- config conflict;
- dataset and matched-initialization contract;
- pilot evidence provenance mismatch between machine-capture record and user-verified proposal numeric capture;
- remaining final-thesis gates.

## Current proposal status

```text
Bab I   = evidence-grounded working draft
Bab II  = first-pass source-grounded, modular normalization ready
Bab III = first source-grounded methodology draft
```

Next milestone: generate the research-flow diagram specification and perform cross-chapter consistency/claim audits before producing formatted DOCX/PDF proposal output.
